export interface ProductImport {
    id:number,
    name: string,
    code: string,
    unit: string,
    amount: number,
    unitPrice: number,
    totalPrice: number,
    declarationNumber: number,
    date: string,
    note: string,
}